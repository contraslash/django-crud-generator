from rest_framework.routers import DefaultRouter
from . import (
    viewsets
)


${url_pattern}_router = DefaultRouter

${url_pattern}_router.register(
    r'api/${url_pattern}',
    ${model_name}ViewSet,
    base_name=${url_pattern}
)

urlpatterns += ${url_pattern}_router

